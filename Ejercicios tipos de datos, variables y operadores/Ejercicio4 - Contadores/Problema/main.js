function contador(){
	var number=Number(prompt("Ingresa un número"));
	var one=number+5;
	var two=one+21;
	var three= two-4;
	alert("El número que has ingresado es "+number+"\nEl resultado de los contadores es: "+one+" "+two+" "+three);
}
contador();